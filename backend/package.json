{
  "name": "palsplan-parental-monitor-backend",
  "version": "1.0.0",
  "description": "PalsPlan Parental Monitor — backend server (Express + WebSocket)",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
