services:
  - type: web
    name: palsplan-parental-monitor
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: DASHBOARD_ORIGIN
        sync: false  # Set this in the Render dashboard to your Vercel URL
    autoDeploy: true
